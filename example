

// auto strip whitspace
// pre defined tokens (whats the smallest set of regex that we can pre create) ??
// *, +, ?, etc ??
// initialize with keyword list

_blarg123 = blah      { expr '+' expr }
          | otherBlah { expr '-' expr }
        

{
    type = _blarg123
    kind = blah
    $1 = expr1
    $2 = expr2
}
